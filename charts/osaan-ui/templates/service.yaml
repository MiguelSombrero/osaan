apiVersion: v1
kind: Service
metadata:
  name: {{ include "osaan-ui.svcName" . }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ include "osaan-ui.name" . }}
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: {{ .Values.port }}
  type: ClusterIP
