apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: competence-matching-service-route
  namespace: osaan-dev
  labels:
    app: competence-matching-service
spec:
  to:
    kind: Service
    name: competence-matching-svc
    weight: 100
  port:
    targetPort: 8080
  tls:
    termination: edge
  wildcardPolicy: None
