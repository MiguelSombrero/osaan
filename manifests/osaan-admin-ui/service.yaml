apiVersion: v1
kind: Service
metadata:
  name: osaan-admin-ui-svc
  namespace: osaan-dev
spec:
  selector:
    app: osaan-admin-ui
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
