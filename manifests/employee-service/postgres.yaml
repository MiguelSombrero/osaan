apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: employee-postgres
  namespace: osaan-dev
spec:
  serviceName: employee-postgres
  replicas: 1
  selector:
    matchLabels:
      app: employee-postgres
  template:
    metadata:
      labels:
        app: employee-postgres
    spec:
      imagePullSecrets:
      - name: redhat-registry-pull-secret
      containers:
        - name: osaan-postgres
          image: registry.redhat.io/rhel10/postgresql-16:latest
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: postgres-secret
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: postgres-storage
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: employee-postgres
  namespace: osaan-dev
spec:
  selector:
    app: employee-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  clusterIP: None
