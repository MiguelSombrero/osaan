apiVersion: v1
kind: Service
metadata:
  name: osaan-ui-svc
  namespace: osaan-dev
spec:
  selector:
    app: osaan-ui
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
