apiVersion: v1
kind: Service
metadata:
  name: competence-profile-svc
  namespace: osaan-dev
spec:
  selector:
    app: competence-profile-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
